import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'app_alcool_gasolina',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
